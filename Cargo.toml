[package]
name = "aoc25"
version = "0.1.0"
edition = "2021"

[dependencies]
regex = "1.10"
num-bigint = "0.4"
num-rational = "0.4"
num-traits = "0.2"

## Note: cargo CLI aliases belong in `.cargo/config.toml`, not Cargo.toml.
